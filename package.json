{
  "name": "neft-hub",
  "version": "1.0.0",
  "private": true,
  "description": "One Cloudflare Pages hub for Neft Teacher apps, GitHub navigation, classroom tools, and family learning resources.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-cloudflare-hub.mjs && node scripts/post-build-crusades.mjs",
    "check": "node scripts/build-cloudflare-hub.mjs --check",
    "preview": "npx wrangler pages dev dist"
  },
  "engines": {
    "node": ">=20"
  }
}
